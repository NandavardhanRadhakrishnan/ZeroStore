{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run main.go",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build main.go",
            "console": "integratedTerminal"
        }
    ]
}
